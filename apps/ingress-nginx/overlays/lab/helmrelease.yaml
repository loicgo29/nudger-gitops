apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-nginx
spec:
  interval: 10m
  chart:
    spec:
      chart: ingress-nginx
      version: 4.11.3          # aligne avec ce que tu utilises
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
        namespace: flux-system
  install: {createNamespace: false}
  upgrade: {remediation: {retries: 3}}
  values: {}

