apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: open4goods-recette

resources:
  - ../../base

# Idem, tu pourras ajouter des patchs spécifiques recette ici
# patchesStrategicMerge:
#   - patch-statefulset.yaml
