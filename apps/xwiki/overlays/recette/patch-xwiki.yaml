# apps/xwiki/overlays/recette/patch-xwiki.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: xwiki
spec:
  timeout: 30m
  dependsOn:
    - name: mysql-xwiki
      namespace: ns-open4goods-recette
  values:
    ingress:
      hosts:
        - host: xwiki.recette.nudger.logo-solutions.fr
          paths:
            - path: /
              pathType: Prefix
      tls: [] 
