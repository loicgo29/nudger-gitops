apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xwiki-tls
  namespace: ns-open4goods-recette   # 👈 tu choisis un namespace de référence (ici recette)
spec:
  secretName: xwiki-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - xwiki.recette.nudger.logo-solutions.fr
    - xwiki.integration.nudger.logo-solutions.fr
