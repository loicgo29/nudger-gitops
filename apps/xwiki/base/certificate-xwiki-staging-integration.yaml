apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xwiki-tls
  namespace: ns-open4goods-integration
spec:
  secretName: xwiki-tls
  issuerRef:
    name: letsencrypt-staging   # ⚠️ staging
    kind: ClusterIssuer
  dnsNames:
    - xwiki.integration.nudger.logo-solutions.fr
