apiVersion: v1
kind: Service
metadata:
  name: xwiki
  namespace: ns-open4goods-recette

  labels:
    app: xwiki
    tier: app
    environment: recette
spec:
  selector:
    app: xwiki
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
