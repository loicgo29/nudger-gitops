apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xwiki-data
  labels:
    app: xwiki
    tier: app
    environment: recette
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn-db
  volumeMode: Filesystem
