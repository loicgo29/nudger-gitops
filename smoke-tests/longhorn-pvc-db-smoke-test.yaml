apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-smoke-test-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-db   # ou "longhorn-db" si tu veux tester xfs
