apiVersion: apps/v1
kind: Deployment
metadata:
  name: longhorn-ui
  namespace: ns-longhorn-system
spec:
  template:
    metadata:
      labels:
        security.nudger/allow-rw-rootfs: "true"
      annotations:
        security.nudger/justification: "Longhorn UI (nginx) écrit /var/log/nginx et /var/run"
