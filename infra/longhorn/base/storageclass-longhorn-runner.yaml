apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-runner
provisioner: driver.longhorn.io
reclaimPolicy: Delete
allowVolumeExpansion: true
volumeBindingMode: Immediate
parameters:
  numberOfReplicas: "1"
  dataLocality: "disabled"
  fsType: ext4
  diskSelector: "runner-disk"
