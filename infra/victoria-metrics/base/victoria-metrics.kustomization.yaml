apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: victoria-metrics
  namespace: flux-system
spec:
  interval: 10m
  path: ./infra/victoria-metrics/base
  prune: true
  sourceRef:
    kind: GitRepository
    name: nudger-gitops
    namespace: flux-system
  validation: client
