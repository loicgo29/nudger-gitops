apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: actions-runner-controller
  namespace: arc-systems
spec:
  interval: 5m
  chart:
    spec:
      chart: actions-runner-controller
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: actions-runner-controller
        namespace: flux-system
  install:
    createNamespace: true
  values:
    # Namespace où seront déployés les runners par défaut
    authSecret:
      create: false
    githubWebhookServer:
      enabled: false
