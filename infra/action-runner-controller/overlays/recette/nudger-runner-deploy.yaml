apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: nudger-gitops-recette-runner
  namespace: ns-open4goods-recette
spec:
  replicas: 1
  template:
    spec:
      repository: loicgo29/nudger-gitops
      dockerdWithinRunnerContainer: false
      ephemeral: false   # 👈 désactive le mode éphémère côté CRD
      containers:
        - name: runner
          image: ghcr.io/actions/actions-runner:2.328.0
          env:
            - name: RUNNER_EPHEMERAL
              value: "false"   # 👈 surcharge pour écraser le secret
          envFrom:
            - secretRef:
                name: actions-runner-controller
