apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: nudger-gitops-runner
  namespace: ns-open4goods-recette
spec:
  replicas: 1
  template:
    spec:
      repository: loicgo29/nudger-gitops
      ephemeral: false
      labels:
        - self-hosted
        - nudger-gitops   # 👈 le label attendu par ton workflow
        - recette
      envFrom:
        - secretRef:
            name: actions-runner-controller   # ✅ cherche le secret
