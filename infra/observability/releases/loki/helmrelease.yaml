apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: logging
spec:
  interval: 10m
  chart:
    spec:
      chart: loki-stack
      version: "2.10.2"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
  values:
    grafana:
      enabled: false
      podSecurityPolicy:
        enabled: false
    promtail:
      enabled: false
      podSecurityPolicy:
        enabled: false
    loki:
      enabled: true
      memberlist:
        enabled: false
      podSecurityPolicy:
        enabled: false
      auth_enabled: false
      image:
        repository: grafana/loki
        tag: "2.9.3"
      persistence:
        enabled: false
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
        limits:
          memory: 600Mi
