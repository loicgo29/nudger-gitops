apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: helmrepo-policy-reporter   # ✅ ajoute le préfixe
  namespace: flux-system
spec:
  interval: 1h
  url: https://kyverno.github.io/policy-reporter
