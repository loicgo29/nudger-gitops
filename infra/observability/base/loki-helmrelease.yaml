apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: logging
spec:
  interval: 10m
  chart:
    spec:
      sourceRef: { kind: HelmRepository, name: grafana, namespace: flux-system }
      chart: loki-stack              # 👈 tu utilises bien loki-stack
      version: "2.10.2"              # 👈 corrige la version du CHART (pas de l’image)
  values:
    grafana:
      enabled: false                 # 👈 laisse à false (tu as Grafana à part)
    promtail:
      enabled: true
    loki:
      enabled: true
      auth_enabled: false
      persistence:
        enabled: true
        size: 10Gi
        storageClassName: longhorn
