apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository.yaml
  - helmrepository-grafana.yaml         # ← AJOUT
  - loki-datasource.yaml                # ← AJOUT
  - logging-namespace.yaml              # ← AJOUT
  - promtail-helmrelease.yaml  
  - loki-helmrelease.yaml         # ← AJOUT
