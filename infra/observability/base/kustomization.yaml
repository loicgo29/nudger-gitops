apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository-prometheus-community.yaml
  - helmrepository-grafana.yaml         # ← AJOUT
  - loki-datasource.yaml                # ← AJOUT
  - logging-namespace.yaml              # ← AJOUT
  - promtail-helmrelease-kube-prometheus-stack.yaml  
  - loki-helmrelease-kube-prometheus-stack.yaml         # ← AJOUT
  - policy-reporter-helmrelease-kube-prometheus-stack.yaml
  - policy-reporter-servicemonitor.yaml
  - kube-components-services              # ← ajoute cette ligne
  - kube-components-rbac  
