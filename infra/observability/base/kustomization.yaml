apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system/sources/helmrepository-prometheus-community.yaml
  - helmrepository-grafana.yaml         # ← AJOUT
  - loki-datasource.yaml                # ← AJOUT
  - logging-namespace.yaml              # ← AJOUT
  - ../releases/kube-prometheus-stack  
  - policy-reporter-servicemonitor.yaml
  - kube-components-services              # ← ajoute cette ligne
  - kube-components-rbac  
