apiVersion: v1
kind: Service
metadata:
  name: kube-scheduler-custom
  namespace: kube-system
  labels:
    app: kube-scheduler-custom
spec:
  selector:
    component: kube-scheduler
  ports:
    - port: 10259
      targetPort: 10259
      protocol: TCP
      name: https-metrics
