apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-scheduler-servicemonitor
  namespace: observability
  labels:
    release: kube-prometheus-stack  # ← indispensable si Prometheus l'attend
    kustomize.toolkit.fluxcd.io/name: observability
    kustomize.toolkit.fluxcd.io/namespace: flux-system
spec:
  endpoints:
    - port: https-metrics              # ← correspond à `kube-scheduler-service.yaml`
      scheme: https
      interval: 30s
      tlsConfig:
        insecureSkipVerify: true
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_node_name]
          targetLabel: instance
        - targetLabel: job
          replacement: kube-scheduler
  namespaceSelector:
    matchNames:
      - kube-system
  selector:
    matchLabels:
      app: kube-scheduler-custom       # ← correspond au Service exposant 10259
