# certificate-grafana.yaml (extrait)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: observability
spec:
  secretName: grafana-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod-dns   # <= DNS-01
  dnsNames:
  - grafana.nudger.logo-solutions.fr
