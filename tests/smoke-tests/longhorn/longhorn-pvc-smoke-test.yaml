apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-smoke-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn   # ou "longhorn-db" si tu veux tester xfs
