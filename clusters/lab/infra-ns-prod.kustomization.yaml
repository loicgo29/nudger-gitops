# clusters/lab/infra-ns-prod.kustomization.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-ns-prod
  namespace: flux-system
spec:
  interval: 1h
  path: ./infra/namespaces/base        # <= si ce dossier ne contient QUE prod
  prune: false                         # évite un drop par erreur
  wait: true
  sourceRef:
    kind: GitRepository
    name: gitops
