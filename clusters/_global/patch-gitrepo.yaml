apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: PLACEHOLDER
  namespace: flux-system
spec:
  sourceRef:
    kind: GitRepository
    name: nudger-gitops   # 👈 anciennement flux-system
