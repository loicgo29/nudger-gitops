apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gitops
  namespace: flux-system
spec:
  interval: 100m
  url: https://github.com/loicgo29/nudger-gitops.git
  ref:
    branch: main
  secretRef:
    name: gitops-credentials
  timeout: 60s

