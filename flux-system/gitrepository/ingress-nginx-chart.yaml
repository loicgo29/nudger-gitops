apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: ingress-nginx-chart
  namespace: flux-system
spec:
  interval: 10m
  url: https://github.com/kubernetes/ingress-nginx
  ref:
    branch: main      # ou un tag connu
