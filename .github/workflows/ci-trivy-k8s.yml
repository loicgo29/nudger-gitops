name: 🔍 Trivy (K8s Manifests)

on:
  pull_request:
    paths:
      - 'apps/**'
      - 'clusters/**'
      - '**/*.yaml'
  workflow_dispatch:

jobs:
  trivy-k8s:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Trivy
        uses: aquasecurity/setup-trivy@v0.2.3
        with:
          version: latest
          cache: true

      - name: Run Trivy misconfig scan
        uses: aquasecurity/trivy-action@v0.28.0
        with:
          scan-type: config
          format: sarif
          output: trivy-k8s.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: false
          ignorefile: .trivyignore

      - name: Upload SARIF report
        if: always() && hashFiles('trivy-k8s.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-k8s.sarif

  trivy-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Trivy
        uses: aquasecurity/setup-trivy@v0.2.3
        with:
          version: latest
          cache: true

      - name: Run Trivy image scan
        uses: aquasecurity/trivy-action@v0.28.0
        with:
          scan-type: image
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: false
          # etc. selon ce que tu veux scanner
