name: 🔍 Trivy (K8s Manifests)    # Nom affiché du workflow dans GitHub Actions

on:
  pull_request:                  # Déclenché sur chaque PR
    paths:                       # ... mais seulement si les fichiers modifiés correspondent à :
      - 'apps/**'                # -> Manifests des apps
      - 'clusters/**'            # -> Manifests des clusters
      - '**/*.yaml'              # -> Tous les YAML
  workflow_dispatch:             # Et exécutable à la main depuis l’UI GitHub

permissions:
  contents: read                 # Lecture du repo uniquement
  security-events: write         # Écriture pour publier les résultats de sécurité (SARIF)

jobs:
  trivy-k8s:
    runs-on: ubuntu-latest       # Runner GitHub standard
    steps:
      - uses: actions/checkout@v4  # Récupère le code du repo

      - name: Cache Trivy DB
        uses: actions/cache@v4     # Mise en cache de la base vulnérabilités Trivy
        with:
          path: ~/.cache/trivy     # Répertoire local utilisé par Trivy
          key: trivy-db-${{ runner.os }}-${{ hashFiles('**/*.yaml') }}
                                   # Clé de cache dépendant du contenu YAML
          restore-keys: trivy-db-${{ runner.os }}-
                                   # Fallback si la clé exacte n’existe pas

      - name: Run Trivy (misconfig + secrets)
        run: ./scripts/ci-on-pr/trivy-k8s.sh .
                                   # -> Analyse Kubernetes Manifests + secrets
                                   # -> Échoue si CRITICAL/HIGH trouvés

      - name: SARIF (misconfig)
        if: always()               # Exécuté même si l’étape précédente échoue
        run: |
          trivy config . \
            --format sarif \
            --output trivy-k8s.sarif \
            --exit-code 0 \
            --skip-dirs "smoke-tests" \
            --ignorefile .trivyignore

      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-k8s.sarif
                                   # Envoie le rapport SARIF dans l’onglet "Security" de GitHub
  trivy-images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache Trivy DB
        uses: actions/cache@v4
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ runner.os }}-images-${{ hashFiles('apps/**/kustomization.yaml') }}
          restore-keys: trivy-db-${{ runner.os }}-images-

      - name: Run Trivy (all images in apps)
        run: |
          TRIVY_SEVERITY="CRITICAL,HIGH" TRIVY_EXIT_CODE=1 \
          ./scripts/ci-on-pr/trivy.sh images-in-apps apps
