name: Sanitycheck ARC
on: workflow_dispatch
jobs:
  test-runner:
    runs-on: [self-hosted, nudger-gitops, recette]
    steps:
      - run: echo "✅ Runner ARC fonctionne ! $(date)"
      - run: uname -a
