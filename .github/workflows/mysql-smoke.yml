name: Smoke Test MySQL

on:
  workflow_dispatch:
  push:
    paths:
      - "smoke-tests/**"
      - "scripts/ci/mysql-smoke.sh"
      - ".github/workflows/mysql-smoke.yml"

jobs:
  mysql-smoke:
    runs-on: ubuntu-latest
    env:
      KUBECONFIG: ${{ github.workspace }}/kubeconfig   # 👈 global
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure kubeconfig
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}
        run: |
          echo "$KUBECONFIG_B64" | base64 -d > "$KUBECONFIG"
          echo "✅ Kubeconfig écrit dans $KUBECONFIG"
          kubectl cluster-info

      - name: Run MySQL smoke test (script source de vérité)
        run: |
          chmod +x scripts/ci/mysql-smoke.sh
          scripts/ci/mysql-smoke.sh
